// Be careful, register is important!

var defaultSettings = {

    align: 'b',         // possible values: c / t / l / r / b
    // c means center, t means top, l means left, right means right, b means bottom

    quality: 'high',    // possible values: low / medium / high / autolow / autohigh

    scale: 'default',   // possible values: default / noborder / exactfit / noscale

    wmode: 'window',    // possible values: window / direct / opaque / transparent / gpu

    background: '#6A7495'

}